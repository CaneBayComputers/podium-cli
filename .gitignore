# Build artifacts
packaging/zeltro-cli_*.deb
packaging/zeltro-cli_*_all/
packaging/debian-package/usr/

# Release artifacts (but allow homebrew formula)
releases/linux/
!releases/homebrew/
!releases/homebrew/*.rb
!releases/*.md

# GUI Dependencies
gui/node_modules/
gui/dist/

# Backup Files
backups/

# AI build instructions
INSTRUCTIONS.md

# Local machine config (not shared — each dev has their own)
machines.local

# AI folders
.claude/
.codex/
.gemini/
.aider*

# Inter-session working notes (GUI <-> CLI). Local only -- this repo is public.
CLI_GUI_ISSUES.md
CLI_GUI_FIXES.md
CLI_GUI_COMMS.md
CLI_GUI_ARCHIVE.md
CLI_GUI_TO_CLI.md
CLI_GUI_TO_GUI.md

# Jekyll build output — generated by the docs build, not source.
_site/
docs/_site/
.jekyll-cache/
docs/.jekyll-cache/
vendor/
docs/vendor/
